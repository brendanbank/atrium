{
  "name": "@brendanbank/create-atrium-host",
  "version": "0.19.1",
  "description": "Scaffolder for new atrium host extensions: backend Python package + frontend Vite bundle + compose stack + CI, all wired against atrium's published image and host SDK packages. Run via `npx @brendanbank/create-atrium-host <name>`.",
  "license": "BSD-2-Clause",
  "type": "module",
  "bin": {
    "create-atrium-host": "bin/index.js"
  },
  "files": [
    "bin",
    "src",
    "template",
    "README.md"
  ],
  "scripts": {
    "build": "echo 'create-atrium-host is plain ESM JS; no build step.'",
    "typecheck": "echo 'create-atrium-host is plain ESM JS; no typecheck step.'",
    "test": "node --test test/names.test.js test/render.test.js",
    "smoke": "node bin/index.js smoke-test-host --yes-defaults --no-git --out /tmp/atrium-scaffold-smoke && echo 'smoke ok'"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "kleur": "^4.1.5",
    "prompts": "^2.4.2"
  },
  "devDependencies": {
    "@types/node": "^25.6.0"
  },
  "publishConfig": {
    "access": "public"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/brendanbank/atrium.git",
    "directory": "packages/create-atrium-host"
  }
}
